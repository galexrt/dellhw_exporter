name: test
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  deploy-prod:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      - uses: actions/setup-go@v2
        with:
          go-version: '^1.13'
      - name: Run tests
        run: |
          make test
